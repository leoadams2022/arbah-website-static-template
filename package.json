{
  "name": "arbah-website-html-tamplate",
  "version": "1.0.0",
  "description": "this is a HTML template for the arbah wordpress theme template to be build on ",
  "license": "MIT",
  "author": "walid ali",
  "type": "commonjs",
  "main": "home.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx tailwindcss -i ./input.css -o ./assets/css/output.css --watch",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.4.2",
    "prettier-plugin-tailwindcss": "^0.6.9",
    "tailwindcss": "^3.4.17"
  },
  "dependencies": {
    "@tailwindcss/aspect-ratio": "^0.4.2"
  }
}
